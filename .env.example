# OpenAI Configuration
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o-mini  # Recommended: 15x cheaper, faster, still accurate for email classification
# OPENAI_MODEL=gpt-4o-2024-08-06  # Use only if you need advanced reasoning
OPENAI_MAX_TOKENS=1500
OPENAI_TEMPERATURE=0.1  # Low temperature for consistent classifications

# Email Provider Settings
# Options: gmail, outlook, both
EMAIL_PROVIDER=gmail

# ========================================
# Gmail Configuration (Choose ONE method)
# ========================================

# Method 1: OAuth Desktop Flow (requires browser, interactive)
# Best for: Personal use, development, testing
GMAIL_MCP_COMMAND=uv
GMAIL_MCP_ARGS=run,mcp-gmail
GMAIL_CREDENTIALS_PATH=/path/to/gmail/credentials.json
GMAIL_TOKEN_PATH=/path/to/gmail/token.json

# Method 2: OAuth with Client ID/Secret (headless, automated)
# Best for: Servers, automation, cron jobs
# Get these from: https://console.cloud.google.com/
# GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GMAIL_CLIENT_SECRET=GOCSPX-your-client-secret
# GMAIL_REFRESH_TOKEN=your-refresh-token

# Method 3: Service Account (domain-wide delegation)
# Best for: Google Workspace with admin access
# GMAIL_SERVICE_ACCOUNT_PATH=/path/to/service-account.json
# GMAIL_DELEGATED_USER=user@yourdomain.com

# ========================================
# Outlook/Microsoft 365 Configuration
# ========================================
OUTLOOK_MCP_COMMAND=npx
OUTLOOK_MCP_ARGS=-y,@softeria/ms-365-mcp-server
OUTLOOK_CLIENT_ID=your-azure-client-id
OUTLOOK_CLIENT_SECRET=your-azure-client-secret
OUTLOOK_TENANT_ID=your-azure-tenant-id

# Classification Settings
CATEGORIES_FILE=config/categories.yaml
BATCH_SIZE=10
MAX_EMAILS_PER_RUN=100
CLASSIFICATION_CONCURRENCY=5

# Logging
LOG_LEVEL=INFO
LOG_FILE=email_classifier.log

# TUI Settings
TUI_REFRESH_RATE=4
ENABLE_RICH_TRACEBACKS=true
